import React from 'react';
import { render } from '@testing-library/react';
import { BasicAudioRender } from './audio-render.composition';

it('should render with the correct text', () => {
  expect('');
});
