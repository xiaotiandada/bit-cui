import React from 'react';
import { render } from '@testing-library/react';
import { BasicCardMore } from './card-more.composition';

it('should render with the correct text', () => {
  expect('')
});
