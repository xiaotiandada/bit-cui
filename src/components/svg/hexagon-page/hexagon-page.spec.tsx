import React from 'react';
import { render } from '@testing-library/react';
import { BasicHexagonPage } from './hexagon-page.composition';

it('should render with the correct text', () => {
  expect('')
});
